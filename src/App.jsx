import React from "react";
import SearchBar from "./SearchBar";  // Import the new SearchBar component

const App = () => {
  return <SearchBar />;  // Just render the SearchBar component
};

export default App;
